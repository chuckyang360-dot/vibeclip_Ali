# 本文件仅作仓库根级提示：根目录通常不加载此文件运行业务。
#
# - 后端完整环境变量模板与说明：backend/.env.example
# - 前端完整环境变量模板与说明：frontend/.env.example
#
# 若需在工具链中统一引用，可复制对应目录下的 .env 使用。

# 与前端一致时可用于文档示例（实际开发请改 frontend/.env）
VITE_API_BASE_URL=http://localhost:8000
